import React from "react";
import styles from './header.module.scss'

const Header = props => {
  return (
    <>
      <div className={styles.headerWrp}>this is the header</div>
    </>
  );
};

export default Header;
